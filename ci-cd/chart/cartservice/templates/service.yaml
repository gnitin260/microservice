{{- with .Values.service }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .serviceName }}
spec:
  selector:
    app: cart-service
  ports:
  - name: grpc
    port: {{ .port }}
    targetPort: {{ .targetPort }}
  type: {{ .type }}
{{- end -}}
